import { ResponsiveState } from './responsive-state';
// constants specs

describe('ResponsiveState', () => {

  beforeEach(() => {
    // create a new instance of the ResponsiveState
  });

  describe('getDevicePixelRatio', () => {
    xit('should return the pixelRatio of the device screen', () => {});
  });

});
